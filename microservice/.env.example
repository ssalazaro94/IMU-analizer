# El microservicio no requiere variables de entorno propias por ahora.
# (Hasta 2026-09-14 existía METODO_DETECCION_LADO para elegir entre dos
# formas de corregir el espejo pierna derecha/izquierda; se retiró porque
# el ajuste automático de sentido de la celda 13 del notebook corre siempre
# — ver analysis.py — y una variable ya seteada en Render simplemente se
# ignora, no hace falta borrarla ahí.)
