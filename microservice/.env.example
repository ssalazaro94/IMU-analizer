# Método para corregir el espejo vertical entre pierna derecha e izquierda:
#   pierna -> inversión determinista según el parámetro `pierna` que manda el
#             cliente en cada request (recomendado).
#   auto   -> heurística original del notebook (invierte según qué extremo de
#             la señal filtrada tiene mayor magnitud).
METODO_DETECCION_LADO=pierna
